{
  "name": "xoxa-msg",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "author": "prav-raghu",
  "license": "MIT",
  "description": "A messaging library for Xoxa",
  "devDependencies": {
    "@types/node": "^24.2.0"
  }
}
